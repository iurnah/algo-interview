List_to_BST(0, 0, 2)
	mid = 0 + (2 - 0) / 2 = 1;
	left = List_to_BST("List(0)", 0, 0);
			mid = 0 + (0 - 0) / 2 = 0;
			left = List_to_BST("List(0)", 0, -1);
				return NULL;
			parent = new Tnode(list->data);
			parent->left = left;
			list = list->next;
			parent->right = List_to_BST("list(1)", 1, 2);
								mid = 1 + (2 - 1) / 2 = 1;
								left = List_to_BST("list(1)", 1, 0);
										return NULL;
								parent = new Tnode(list->data);
								parent->left = left(NULL);
								list = list->next;
								parent->right = List_to_BST("list(2)", 2, 2);
												mid = 2 + 0 = 2;
												left = List_to_BST("list(2)", 2, 1)
														return NULL;
												parent = new Tnode(list->data);
												parent->left = left;
												list = list->next;
												parent->right = List_to_BST("list(NULL)", 3, 2)	
																	return NULL;
												return parent("list(2) = 9");
								return parent("list(1) = 8");

			return parent("list(0) = 5");

	parent = new Tnode(list->data);
	parent->left = left;
	list = list->next;
	parent->right = List_to_BST("list(X)", 2, 2);
	return parent;

	**********
	mid = 0 + (2 - 0) / 2 = 1;
	left = List_to_BST("List(0)", 0, 0);
	parent = new Tnode(list->data);
	parent->left = left;
	list = list->next;
	parent->right = List_to_BST("list(X)", 2, 2);
	return parent;
